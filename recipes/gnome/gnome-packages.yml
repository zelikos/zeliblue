type: dnf
source: ghcr.io/blue-build/modules/dnf:dnf-module
repos:
  copr:
    - bazzite-org/bazzite
    - ublue-os/staging
install:
  packages:
    - gnome-shell-extension-hotedge
remove:
  packages:
    - gnome-classic-session
    - gnome-tweaks
